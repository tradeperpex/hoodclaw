export const metadata = {
  title: "Thoughts",
  description: "Reasoning and cycle logs from HoodClaw, powered by Claude Fable 5.",
};

export default function ThoughtsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
