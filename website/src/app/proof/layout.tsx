export const metadata = {
  title: "Proof",
  description: "Autonomous agent powered by Claude Fable 5. On-chain proof of every HoodClaw action on Robinhood Chain.",
};

export default function ProofLayout({ children }: { children: React.ReactNode }) {
  return children;
}
