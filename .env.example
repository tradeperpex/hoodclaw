# AgentClaw — copy website/.env.example to website/.env.local for local dev.
# All env vars live in the website/ folder (Vercel root directory = website).

See website/.env.example for the full list.
